Nordstjernen 1.0.17 — development-cycle plan
============================================

Snapshot: **1.0.17**, 2026-06-29. A prioritized focus list and final
cycle ledger for 1.0.17, derived from a full pass over the source tree
and the project's own scoreboards. Every item below is anchored to
concrete `file:line` evidence so it can be checked before it is picked up.

The spine of the cycle is six high-conviction moves, in order, followed
by a "finish the half-built" tail and a set of scoped down-payments on
the big multi-cycle items. Because the project ships **no automated test
suite by policy**, every change is a manual-verify event — the single
most leveraged item here (rank 4) is the closest thing to a safety net
that still respects that policy.

Status legend: severity (high/med/low) · effort (small/med/large) · ROI.

**Progress (2026-06-27):** ranks 2–4 shipped in 1.0.17-dev — proxy leaks +
renderer propagation (`c04c432`), final-redirect CSP/mixed-content re-check
(`4dc609b`), and the ASan/UBSan + libFuzzer safety net (`a50dcd2`, hardened
in `0f18b66`). The security cluster is closed and the perf work is unblocked.
Quick wins landed (`a46f92c`): microtask drain-to-empty, the Android stub-APK
CI guard, and macOS/Qt behavioural smoke gates. Rank 7 (`fba3262`): dom/events
passive-by-default + body→window handler reflection (activation deferred).
Rank 8 (`b65631a`, hardened `fdea766`): the Selection API on `NdRange`
(programmatic selection now works; the C stub stays as the engine-highlight
fallback; boundary points and cross-tree extents are now validated). Rank 10
(4/5): `crypto.subtle.wrapKey`/`unwrapKey` (`ab4a356`) and the aggregate
per-origin IndexedDB cap (`222dbbe`), plus `CompressionStream`/
`DecompressionStream` and the microtask drain found already shipped —
leaving only `video.currentTime` (deferred: crosses the renderer↔audio
boundary, not headless-verifiable here).

**Post-release update (2026-06-29):** additional high-ROI hardening landed
after the `release: 1.0.17` tag point. Commit `7135197` tightened renderer
and media IPC/network bounds: EventSource and the audio helper now pin
allowed protocols and redirect schemes more tightly, audio downloads are
bounded, and renderer HTTP drop-file/request parsing rejects negative or
overflowing lengths. Commit `2b71c0d` shipped an initial MSE append path:
`MediaSource`, `SourceBuffer`, and `SourceBufferList` are present; appended
bytes update a native blob URL that the existing media loader can fetch; and
inline video discovery accepts `blob:` URLs. This is a real YouTube/DASH
compatibility down-payment, not yet a full adaptive media stack.


## Headline themes

1. **Make the WPT scoreboard honest, then aim at the one real lever
   (URL/IDNA).** `scripts/wpt-score.sh` sums non-passing subtests with no
   `/tentative/` exclusion, so the ROI table's top rows are inflated by
   explicit non-goals — `dom/ranges` "gain 180" is ~179 tentative
   `OpaqueRange` subtests, `WebCryptoAPI/digest` "gain 371" is tentative
   cSHAKE/KangarooTwelve/TurboSHAKE that the doc itself calls out of scope
   (`docs/wpt-scores.md:108,111,149`). After the filter, the genuine top
   lever is URL/IDNA (~870 real `toascii` + `IdnaTestV2` subtests) in the
   heavily-modified in-tree lexbor `idna.c`.

2. **Close the security cluster: proxy leaks first, then redirect
   re-validation.** WebSocket / EventSource / the audio helper apply TLS
   but never the configured proxy, silently defeating the documented proxy
   recipe. Redirect re-validation splits into a cheap final-URL re-check
   (lands now) and a larger per-hop loop (deferred).

3. **Build the safety net before the risky perf work.** Three audit
   commits in one day fixed the same length-overflow / detach-during-
   `valueOf` / unbounded-queue pattern by hand; there is zero
   asan/ubsan/fuzz/valgrind tooling. A sanitizer build of the *existing*
   headless+WPT path is not a test suite and respects the no-tests-dir
   policy — and it must land before the `:has()` restyle scoping, whose
   intermittent stale-paint failure mode is exactly what manual
   verification misses worst.

4. **Auditability is a stated design constraint, not a nice-to-have.**
   `src/js.c` is 40,373 lines and owns timers, workers, XHR/fetch, custom
   elements, WebCrypto marshalling and iframe scoping. CLAUDE.md elevates
   "readable and auditable by a single human" to a design constraint, and
   the recurring security bugs land in exactly this file — so a scoped
   one-subsystem extraction earns a real rank, not a footnote.

5. **Finish the half-built.** Selection is a live no-op C stub (Range is
   fully built); module workers throw; `video.currentTime` seek,
   `wrapKey`/`unwrapKey`, and `CompressionStream` advertise support they
   don't deliver. Each is contained and high-payoff.


## Priority table

| # | Initiative | Kind | Sev | Effort | ROI |
|---|------------|------|-----|--------|-----|
| 1 | Filter `/tentative/` + non-goal algorithms from the WPT ROI generator, then re-cluster the Top-10 | docs | high | small | high |
| 2 | ✅ **done** — Route WebSocket / EventSource / audio helper through the configured proxy and app TLS/CA | security | high | small→med | high |
| 3 | ✅ **done** — Re-validate CSP / mixed-content on the **final** redirect URL (cheap increment) | security | high | small | high |
| 4 | ✅ **done** — Stand up an ASan/UBSan headless build + a libFuzzer fuzz harness | security | high | med | high |
| 5 | Extract one cohesive subsystem out of the 40,373-line `src/js.c` (workers → `js_worker.c`) | maintainability | med | med | med |
| 6 | Fix the in-tree lexbor IDNA/UTS-46 path (top honest compliance lever) | compliance | high | large | high |
| 7 | 🟡 **2/3 done** — `dom/events`: passive-by-default ✅, body→window reflected handlers ✅, activation behavior (deferred) | compliance | high | med | high |
| 8 | ✅ **done** — Implement the Selection API on the existing `NdRange` polyfill | feature | high | med | high |
| 9 | Module workers (`new Worker(url, {type:'module'})`) | feature | med | med | med |
| 10 | 🟡 **4/5 done** — correctness bundle: `wrapKey`/`unwrapKey` ✅, `CompressionStream` ✅ (already shipped), microtask drain ✅, aggregate IDB cap ✅; remaining: `video.currentTime` (deferred — crosses renderer↔audio boundary, not headless-verifiable) | correctness | med | med | high |
| 11 | Hygiene: de-truncate depth-512 DOM walkers, refresh stale compat docs, prune dead C stubs | correctness | med | med | med |


## Ranked initiatives

### 1 — Make the WPT scoreboard honest  ·  docs · high · small · high

The ROI table is the maintainer's primary "what to work on" artifact, and
its top two rows misdirect toward documented non-goals.

- **Evidence:** `scripts/wpt-score.sh` sums the non-pass count
  (`FAIL`/`TIMEOUT`/`NOTRUN`/`PRECONDITION_FAILED`) with no `/tentative/`
  exclusion. `docs/wpt-scores.md:111` ranks `dom/ranges` "gain 180"
  (~179 of which are `/dom/ranges/tentative/OpaqueRange-*`) and `:108`
  ranks `WebCryptoAPI/digest` "gain 371" (tentative cSHAKE / KangarooTwelve
  / TurboSHAKE that `:149` already calls out of scope).
- **First steps:** exclude subtest paths containing `/tentative/` (and the
  named SHA-3-family digest vectors) from the non-pass sum; regenerate
  `docs/wpt-scores.md` and `docs/wpt-subtests.tsv` via a full
  `scripts/wpt-score.sh --wpt-root=~/wpt` run; re-rank so URL/IDNA surfaces
  at its true position. The code change is small — **budget the full WPT
  re-run as the real cost.** Do this first so everything after is aimed
  correctly. (Don't quote a headline "~540 non-goal subtests": it is
  179 + 371 from two different run contexts — directionally right, not a
  single verified number.)

### 2 — Route WebSocket / EventSource / audio through the proxy + app TLS  ·  ✅ DONE (1.0.17-dev)

A silent deanonymization leak that directly contradicts shipped docs.

- **Status — done (commit `c04c432`).** Both `ws.c` worker paths and
  `eventsource.c` now apply `ns_net_apply_curl_proxy`; the audio helper is
  spawned with the proxy + CA in its environment (`procview.c`). The
  `--proxy` override turned out to be more than "small": it only lived in
  the shell's `g_proxy_override` and never reached the per-tab **renderer**
  (a separate process) that actually opens WebSocket/EventSource — so the
  fix also exports `NS_HTTP_PROXY`/`NS_HTTPS_PROXY`, which `config.c` loads
  in every process. Hardening: `wss://` is now treated as TLS in the proxy
  picker, and the audio override forces the scheme-specific vars so an
  inherited `http_proxy` can't win. **Verified** at runtime with a logging
  proxy in both single- and multi-process mode — wss/ws/SSE all route
  through the proxy. **Effort was med, not small** (the renderer-propagation
  gap an adversarial review caught). **Follow-on still open:** fail-closed
  proxy egress + the Landlock net jail — this fix
  *routes* through the proxy but does not yet *refuse* direct connections.

- **Evidence:** `src/ws.c:437` and `:939`, and `src/eventsource.c:324`,
  call `ns_net_apply_curl_tls` but never `ns_net_apply_curl_proxy`; the
  audio helper (`src/audio/main.c`) calls neither. The helper exists at
  `src/net.c:1843` and is wired only by the main fetch at `net.c:4943`.
  `docs/Proxy.md:144-145` explicitly promises WebSocket is tunneled through
  the same proxy. On the documented proxy recipe
  (`--proxy=socks5h://127.0.0.1:9050`) every `ws://`, `wss://`, SSE and
  remote-audio connection currently goes out direct, because libcurl only
  auto-detects lowercase env proxies, not the browser's `--proxy` flag.
- **First steps:** add `ns_net_apply_curl_proxy(curl, url)` after the
  existing TLS apply at each of the ~4 `curl_easy_init` sites, plus
  `ns_net_apply_curl_tls` in the audio helper; smoke-test with
  `--proxy=socks5h://…` against a `wss` echo and an SSE endpoint.

### 3 — Re-validate CSP / mixed-content on the final redirect URL  ·  ✅ DONE (1.0.17-dev)

The cheap, lands-now half of redirect re-validation.

- **Status — done (commit `4dc609b`).** `fetch()` and XHR now re-run the
  connect-src CSP + mixed-content check on `resp->final_url` before delivery
  (shared `ns_final_url_connect_blocked` helper): fetch rejects, XHR yields an
  opaque blocked response; dedicated workers already did this. **Scope
  landed:** fetch + XHR (the connect-src paths); `img-src` subresources and
  full per-hop enforcement remain the deferred larger work. **Verified** at
  runtime — a `connect-src` page whose same-origin fetch 302-redirects to a
  disallowed origin is blocked, the body never delivered.
- **Evidence:** `src/js.c:6470-6492` checks mixed content and
  `ns_csp_allows(NS_CSP_CONNECT,…)` only on the pre-redirect URL, then
  `net.c:4267` sets `CURLOPT_FOLLOWLOCATION` and the final URL is never
  re-validated. `net.c` already exposes `resp->final_url` (`:4568`) and
  `location_out` (`:4663`).
- **Nuance:** the worker loader (`js.c:15215`) re-checks *only* the final
  URL after curl has already followed the whole chain — it does **not**
  validate intermediate hops, so a `self→attacker→self` redirect still
  passes. So "mirror the worker" is the **cheap increment**, not the full
  fix: re-running CSP + mixed-content + SafeBrowsing on `resp->final_url`
  catches the common `https→http` downgrade and final-origin exfil and
  reuses machinery `net.c` already exposes. Full per-hop enforcement is
  separately-scoped larger work, deferred.
- **First steps:** for fetch/XHR/`img-src` subresource fetches, after the
  request returns, re-run the allow-checks on `resp->final_url` before
  delivering the body; verify with a page that fetches an `https→http` and
  a same→cross-origin redirect.

### 4 — ASan/UBSan headless build + libFuzzer harness  ·  ✅ DONE (1.0.17-dev) · later removed

The missing safety net. **Landed before the `:has()` work (big bet 2).**
**Subsequently removed to keep the project lean** — the `sanitize.yml` CI job,
`scripts/sanitize.sh`, `scripts/fuzz.sh`, and the `fuzz/` harnesses are gone;
`data/render-tests/` remains (still used by `scripts/render-tests.sh`). The
description below records what existed.

- **Status — done (commit `a50dcd2`).** Without a `tests/` dir:
  `scripts/sanitize.sh` runs an ASan+UBSan build over the existing in-process
  `--headless --dump` path across `data/render-tests`; `fuzz/fuzz_html.c` +
  `fuzz/fuzz_url.c` + `scripts/fuzz.sh` are coverage-guided libFuzzer
  harnesses over the lexbor HTML and WHATWG URL parsers (linked against a
  SanitizerCoverage-instrumented liblexbor); `.github/workflows/sanitize.yml`
  is an advisory, non-blocking CI job. **Verified** locally — both fuzzers
  build and run coverage-guided (HTML 1267→4211 edges, URL 336→1709) with no
  findings. **This satisfies the sequencing constraint and unblocks the
  `:has()` perf work.** Next fuzz targets (the plan's priority): the CSS
  tokenizer and the `src/ext.c` filter-list parser.
- **Evidence:** a grep for `asan|ubsan|fuzz|valgrind|sanitize` across
  `meson.build` and `scripts/` returns nothing. Three audit commits in one
  day (`0dea3e5`, `3d9f1a3`, `9421b60`) fixed the same overflow / detach /
  unbounded-queue pattern by hand; ~185 reactive depth-guard sites exist in
  `src/js.c`. `src/ext.c` (1251 lines, 4 commits old) parses
  attacker-adjacent remote EasyList/ABP bytes.
- **Why:** the maintainer is catching this bug class one-at-a-time by
  manual audit because nothing systematically exercises the binding/parser
  surface. A `-Db_sanitize=address,undefined` build over the existing
  headless WPT slice, plus a small libFuzzer harness over `ns_url_parse` /
  the HTML parser / the CSS tokenizer / JS argument marshalling, converts
  recurring hand-finds into build-time failures. It sanitizes the
  **existing** run path — not a new test suite.
- **First steps:** add a `scripts/` wrapper that configures a second
  builddir with sanitizers and runs the headless WPT slice under it; add a
  tiny libFuzzer target and point it at `src/ext.c` filter-list parsing
  first; wire the sanitizer build as a non-blocking CI job.

### 5 — Extract one subsystem out of the 40,373-line `src/js.c`  ·  maintainability · med · med · med

A scoped down-payment on the project's biggest auditability violation —
which directly reinforces rank 4 (it is the file most bugs land in).

- **Evidence:** `wc -l src/js.c` = 40,373 (~3,463 function-like
  definitions); the next-largest source is `css.c` at 15,760. It owns
  timers, workers, XHR/fetch, custom elements, all DOM bindings, WebCrypto
  marshalling, iframe scoping and the JS pump. The project already splits
  `js_intl.c` / `js_canvas.c` / `js_perf.c` via `subdir()`.
- **First steps:** carve the **workers** subsystem (`ns_worker_*` + the
  worker realm bridge) into `src/js_worker.c` with a small internal header
  for the shared statics it touches; wire via `subdir()` exactly as
  `js_intl.c` is; confirm clean compile + headless smoke + the rank-4
  sanitizer run. No behavior change. Defer `js_fetch.c` / `js_ce.c` to
  later cycles.

### 6 — Fix the in-tree lexbor IDNA/UTS-46 path  ·  compliance · high · large · high

After rank 1, the single biggest honest score lever — pure standards
conformance, no scope ambiguity.

- **Evidence:** `docs/wpt-scores.md:107` `url` = 1048 gain, highest
  gain/file in the slice. ~870 of that is genuine: `/url/IdnaTestV2` = 775
  non-pass, `/url/toascii` = 63, both standard UTS-46/IDNA (not tentative).
  The in-tree lexbor `unicode/idna.c` is heavily locally modified, so the
  failures are partly our own; the 2026-06-26 lexbor 3.1.0 refresh already
  pulled empty-host setter fixes.
- **First steps:** run `scripts/wpt-score.sh --wpt-root=~/wpt url` to
  isolate failing `IdnaTestV2`/`toascii` subtests; diff `unicode/idna.c`
  against upstream lexbor 3.1.0 to find local-modification regressions; fix
  the UTS-46 mapping/validity path; rerun just `url` and commit the
  regenerated scoreboard with the engine change.

### 7 — The `dom/events` compliance cluster  ·  🟡 2/3 DONE (1.0.17-dev)

Three bounded changes to an already-correct dispatch core, high WPT
density (298 available gain across 62 files).

- **Status — 2 of 3 done (commit `fba3262`).** **Passive-by-default** and
  **body→window handler reflection** shipped and verified headless (window
  `wheel` `preventDefault` ignored while explicit `passive:false` and
  non-root targets still cancel; `body.onX === window.onX`, forwards,
  enumerates, and fires exactly once). **Activation behavior is deferred:** an
  investigation found the scripted paths (`element.click()`, `dispatchEvent`)
  are already spec-correct; the residual deviation is a narrow
  trusted-mouse-click case, the fix touches three files in lockstep (`js.c`,
  `libnordstjernen.c`, `headless.c`) across the shell, and it **cannot be
  verified headlessly** (no real mouse input). Not worth the blast radius
  now — revisit with a real-UI click / `<label for>` / radio-group smoke
  test.

- **Evidence:** `dom/events` is the weakest DOM area (106/168 files,
  521/819 subtests, 228 fail). Three clusters: `passive-by-default.html`
  68/100 ERROR, `Body-FrameSet-Event-Handlers` 12/48,
  `Event-dispatch-single-activation-behavior` 85/132.
  `ns_event_type_is_window_reflected` (`js.c:18740`) exists but there is no
  default-passive rule and no `body`→`window` `on*` reflection. The
  capture/at-target/bubble + `composedPath` core is already spec-correct.
- **First steps:** default `wheel`/`touchstart`/`touchmove`/`mousewheel`
  listeners on window/document/body to passive; reflect `body` on-handlers
  for window-reflected events to the window; consolidate activation
  behavior; rerun `dom/events` between sub-fixes.

### 8 — Selection API on the existing `NdRange` polyfill  ·  ✅ DONE (1.0.17-dev)

- **Status — done (commit `b65631a`, hardened `fdea766`).** A real
  `NdSelection` (singleton) layered on `NdRange` in `data/js/polyfills.js`:
  `collapse`/`setPosition`, `collapseToStart`/`End`, `extend`,
  `setBaseAndExtent`, `selectAllChildren`,
  `addRange`/`removeRange`/`removeAllRanges`/`empty`, live `getRangeAt`,
  `containsNode`, `deleteFromDocument`, `toString` — with correct
  anchor/focus/direction and the spec `IndexSizeError`/`InvalidStateError`
  throws. The C `getSelection` was **kept as a fallback** (not deleted): when
  no range is set programmatically the readers delegate to it, so the engine
  user-highlight `toString`/`getRangeAt` still work. Pure-JS change, no C.
  **Verified** headless on a `contenteditable` page (selectAllChildren,
  collapse, forward+backward extend, setBaseAndExtent, getRangeAt identity,
  add-when-occupied, and the error/edge cases).
- **Hardening (`fdea766`, post-review).** `collapse`/`extend`/`setBaseAndExtent`
  now route offsets through the same `checkBoundary` the Range setters use, so a
  negative/over-length offset throws `IndexSizeError` and a `DocumentType` node
  throws `InvalidNodeTypeError` (previously `offset | 0` stored a bad offset
  silently); `extend`/`setBaseAndExtent` whose anchor and focus live in
  different trees now throw `WrongDocumentError` instead of building an
  incoherent backward range from a meaningless boundary comparison. A 26-case
  headless guard suite (boundary errors, cross-tree guards, forward/backward
  extent, addRange identity, removeRange) is green. **Deliberately kept**
  `getRangeAt` returning the *live* `_range`, not a clone: the review suggested
  cloning to insulate the internal direction snapshot, but cloning would break
  the spec/browser `addRange(r); getRangeAt(0) === r` identity **and**
  `removeRange(getRangeAt(0))` — two concrete round-trips — to guard a
  theoretical, browser-divergent desync. Live range wins.
- **Scope cut:** `selectionchange` events are not yet fired on programmatic
  mutation, and a prior programmatic selection shadows later user highlights
  until `removeAllRanges()` — both need a C-side selection-change signal
  (follow-up, out of scope for a JS-only change).
- **Evidence:** `src/js.c` binds all Selection methods
  (`addRange`/`collapse`/`extend`/`setBaseAndExtent`/`getRangeAt`) to the
  no-op handler with `anchorNode`/`focusNode` always `null`; `Selection`
  appears **0** times in `data/js/polyfills.js`, so unlike Range there is
  no polyfill override — the no-op C stub is the live implementation.
- **Why:** rich-text editors, copy-helpers and highlight features call
  Selection; `getSelection().toString()` works but everything stateful is
  silently dropped. Because Range already exists in `polyfills.js`
  (`NdRange`), a Selection polyfill layers on the same machinery.
- **First steps:** add a Selection polyfill in `data/js/polyfills.js`
  backed by `NdRange`/`__ndCreateRange`, route `getSelection()` to it, and
  retire the dead C no-op stub; verify with a `contenteditable` page
  calling `collapse`/`extend`/`getRangeAt`.

### 9 — Module workers  ·  feature · med · med · med

- **Evidence:** `src/js.c:16241` throws
  `TypeError: Worker: module workers are not supported` for any non-classic
  type. Dedicated classic workers (with `importScripts`/`fetch`/
  `crypto.subtle`) already work, and the engine already runs module scripts
  on the main realm (`ns_script_type_is_module`, `js.c:38334`).
- **Why:** `new Worker(url, {type:'module'})` is now the default in Vite
  and bundleless-ESM tooling, so the classic-only limitation silently
  breaks a growing share of real sites. Both halves (worker plumbing,
  module evaluation) already exist; the increment is joining them.
  SharedWorker stays unsupported.
- **First steps:** in the Worker constructor, accept `type:'module'` and
  route the entry script through the existing module-evaluation path in the
  worker realm; verify with a Vite-style
  `new Worker(new URL('./w.js', import.meta.url), {type:'module'})` that
  imports a sibling ESM module.

### 10 — Small high-confidence correctness bundle  ·  correctness · med · med · high

Self-contained fixes, each closing a latent gap where the surface
advertises support it doesn't deliver.

- **`video.currentTime` seek:** `js.c:30261` is a no-op setter;
  `video_decode.c:194` only seeks to 0. Wire a `seek <token> <t>` command
  from a `currentTime` assignment to `av_seek_frame` and the audio helper.
  (Deferred: crosses the renderer↔audio-helper boundary and is not
  headless-verifiable in this environment.)
- **`wrapKey`/`unwrapKey`:** ✅ **done (commit `ab4a356`).** Implemented as
  spec compositions over the existing primitives — `wrapKey` = export
  (raw/pkcs8/spki bytes or JSON-serialised JWK) + `encrypt`; `unwrapKey` =
  `decrypt` + import — with the wrapping-key usage and target-key
  extractability checks (`InvalidAccessError` otherwise). The `importKey`
  body was factored into `ns_wc_do_import` and the cipher params into
  `ns_wc_do_cipher` so both operations reuse proven code; bound at both
  `crypto.subtle` install sites. Verified headless: AES-GCM raw + JWK
  round-trips, RSA-OAEP public-wrap/private-unwrap, GCM auth-tag tamper
  rejection, non-extractable and missing-usage error paths. (AES-KW is not
  added — `webcrypto.c` has no RFC-3394 path; AES-GCM/CBC/CTR and RSA-OAEP
  wrapping cover the common cases.)
- **`CompressionStream`/`DecompressionStream`:** ✅ **already shipped** (not
  outstanding — earlier plan text was stale). A real zlib codec
  (`__ns_zlib_create`/`push`/`finish`, `js.c:33229`) is wrapped by
  `TransformStream` subclasses with a `zlibTransformer` for
  gzip/deflate/deflate-raw (`polyfills.js:1122-1171`), and the
  `polyfills-verify` step asserts both constructors exist
  (`verify-polyfills.js:91`). Nothing to do.
- **Microtask drain cap:** ✅ **done** (`a46f92c`) — the `int safety = 100000`
  counter was removed; the drain now relies on the existing
  `eval_deadline_us` wall-clock guard.
- **Aggregate IDB cap:** ✅ **done (commit `222dbbe`).** Per-database pages
  were capped (`NS_IDB_MAX_PAGES = 65536`) but an origin could open unlimited
  named DBs, leaving the per-origin total unbounded. `ns_idb_backend_put` now
  sums `PRAGMA page_count` across the partition's databases before commit and
  rejects with `QuotaExceededError` past `NS_IDB_MAX_ORIGIN_PAGES` (1 GiB
  default, env-overridable); the over-limit write rolls back and leaves other
  DBs intact. The current DB is measured via its live (in-transaction)
  connection, the rest via a short-lived read-only handle like `databases()`.
  Enforced at `put` only — the sole large-data path; `createStore`/`createIndex`
  add negligible schema rows and stay ungated to avoid an O(N) scan per DDL.
  **Verified** headless: aggregate trip (db1 fills under cap, a small db2 write
  trips because the *origin* total crosses the cap), `QuotaExceededError` name,
  rollback of the failed record, db1 unharmed, and normal writes succeeding
  under the default cap — all clean under ASan/UBSan/LSan.

### 11 — Correctness / doc hygiene  ·  correctness · med · med · med

- **Evidence:** `NS_DOM_MAX_DEPTH = 512` (`dom.c:1011`) truncates
  script-reachable walkers — `collect_text` (`:1920`, backs
  `textContent`), `serialize_node` (`:1978`), `clone` (`:1014`), and the
  id/class/tag index builders (`:1196`, `:1437`, `:1536`) — so deep
  documents get wrong `textContent`/serialization/clone and elements
  invisible to `getElementById`. `CSS-compatibility.md:15` is six releases
  stale (1.0.10) with a false `content-visibility` ❌ row (it is
  implemented at `layout.c:310,9375`).
- **First steps:** convert the half-dozen script-reachable walkers to
  iterative + explicit-stack like `ns_node_free` already is; re-check every
  ❌/🟡 row in `CSS-compatibility.md` against current source (correct
  `content-visibility`, downgrade flex baseline to 🟡) and bump the
  snapshot; delete or sign-post the dead C no-op Range/Selection stubs.


## Quick wins

Small, high-confidence, mostly independent of the above:

- The rank-1 `/tentative/` filter at the non-pass sum (code is small;
  budget the WPT re-run for the doc regen).
- The rank-2 one-line `ns_net_apply_curl_proxy` calls at each
  `curl_easy_init` site (closes the proxy leak).
- The rank-3 `resp->final_url` re-check for fetch/XHR.
- ✅ *Done (`7135197`):* added `CURLOPT_PROTOCOLS_STR`/
  `CURLOPT_REDIR_PROTOCOLS_STR` to EventSource and the audio helper with
  conditional `https` redirect pinning, so plain-http SSE remains valid
  while `https` media/SSE cannot be redirected to cleartext. The same
  hardening capped audio-helper downloads and rejected negative or
  overflowing renderer HTTP payload lengths.
- ✅ *Done (`a46f92c`):* removed the redundant 100k microtask-drain counter
  (`js.c`) — the checkpoint now drains to empty under the existing wall-clock
  `eval_deadline_us` (and the watchdog backstop), instead of dropping pending
  jobs past 100k.
- ✅ *Done (`a46f92c`):* Android CI guard — dropped `continue-on-error` from
  the sysroot fetch and assert each `jniLibs/<abi>/libnordstjernen.so` exists
  before `assembleDebug`, so a stub APK (`nativeEngineAvailable=FALSE`) can no
  longer ship green.
- ✅ *Done (`a46f92c`):* behavioural smoke gate for the shipping shell — GTK
  `--headless --dump=text about:start` in `macos.yml`. (The Qt offscreen
  smoke in `qt.yml` was dropped when the experimental Qt 6 frontend was
  removed.)
- Refresh `CSS-compatibility.md` (six releases stale): fix the false
  `content-visibility` ❌ row, downgrade flex baseline to 🟡.
- Bind `createConicGradient` in `js_canvas.c` (`conic-gradient()` already
  exists in the CSS path) — closes the last well-known Canvas 2D gap.
- Normalize the raw ellipsis bytes in `ns_i18n()` keys (`procwindow.c:1859`)
  to literal UTF-8 `…` to de-risk catalogue key matching.


## Big bets (scoped down-payments, not single-cycle big-bangs)

- **lexbor IDNA/UTS-46** (rank 6) — the biggest honest WPT lever, partly
  self-inflicted via the locally-modified `unicode/idna.c`.
- **Scope the `:has()`/`@container` incremental-restyle disable** to
  affected subtrees instead of the whole page (`css.c:15695-15700`) to
  restore the already-built −28% fast path. **Treat this as a research
  spike, not a confident "restore the machinery":** correct `:has()`
  *invalidation-set* computation is the hardest problem in incremental CSS
  (a deep change can flip an arbitrary ancestor's match state), and
  `g_has_memo` memoizes match *results*, not the invalidation set. Its
  failure mode is intermittent stale paint — so it **must follow rank 4**
  and sits below the security cluster, not parallel to it.
- **Subtree-local relayout:** every restyle currently rebuilds the entire
  layout tree (`libnordstjernen.c:137` → `engine.c:404`), and hover is
  rate-limited to the relayout cost as a band-aid
  (`libnordstjernen.c:1056`). Dirty-flag the changed box's containing block
  and re-lay just that. Large surgery, high everyday-interaction payoff;
  natural follow-on to the `:has()` scoping.
- **Credential-less renderer broker** (`tab-isolation.md` steps 3-7): the
  renderer still holds the cookie jar, cache, history and full network
  egress (`libnordstjernen.c:467-469`; seccomp permits `socket`/`connect`).
  Too large for one cycle — the right 1.0.17 down-payment is **specifying
  the broker IPC message surface** (step 6), not building it.
- **i18n fill-in:** only 27 of 98 `ns_i18n` strings are translated even in
  the best (German) catalogue, and 27 of 40 `.lang` files are 2-key
  placeholders. Mechanical, high-visibility for an OS-language-aware
  browser; pair with a `scripts/` extract-and-diff helper so coverage is
  CI-measurable, and prune or flag the placeholder languages.


## Headline ambitions: YouTube and a Communicator-style mail client

Two large, explicitly-requested features. They differ enormously in
feasibility and in how they sit against the project ethos, so the scope is
documented honestly here. Both are product-scope decisions, not feature
drops. **Update (1.0.17-dev):** the mail client's MVP + compose has now
shipped as `about:email` (commit `67aba1f`, see below). **Update
(2026-06-29):** the prior YouTube/MSE non-goal was explicitly overridden
for a narrow compatibility slice; commit `2b71c0d` shipped an initial MSE
append path, while full adaptive DASH remains a large media-stack decision.

### "Working YouTube"  ·  initial MSE append path shipped, full DASH remains large

Reality check: `youtube.com` does not serve a plain file a `<video src>`
can play. It streams adaptive DASH segments through **MSE**
(`MediaSource`/`SourceBuffer`) in VP9/AV1/AVC, often with audio and
video delivered as separate tracks. A narrow MSE path now exists, but
"embedded, working YouTube" is still not a small feature — it is a decision
to build and maintain much more of a media stack.

**What DASH and MSE are.** *MSE (Media Source Extensions)* is a JavaScript
API (`MediaSource` / `SourceBuffer`) that lets a page's own script feed
encoded media bytes into a `<video>` element chunk by chunk, instead of
pointing it at a file with `src` — the page, not the browser, drives the
fetching and buffering:

```js
const ms = new MediaSource();
video.src = URL.createObjectURL(ms);            // JS-driven source
const sb = ms.addSourceBuffer('video/mp4; codecs="vp9"');
sb.appendBuffer(nextSegmentBytes);              // hand the decoder bytes
```

*DASH (Dynamic Adaptive Streaming over HTTP, MPEG-DASH)* is the matching
delivery format: the video is cut into short segments (~2–10 s) encoded at
several quality levels, all listed in an XML manifest (an MPD). The player
fetches segments one at a time and switches quality to match the current
bandwidth ("adaptive bitrate"), so a slow link drops to a lower rendition
instead of stalling. (HLS is Apple's near-identical `.m3u8` equivalent.)
YouTube combines the two — its player uses MSE to append DASH segments — so
there is **no single URL** a `<video src>` can play; the "video" exists only
as the player stitching thousands of segments together at runtime. That is
why supporting it means a fragmented-MP4/WebM demuxer that accepts arbitrary
appended byte ranges, plus A/V sync, cross-segment seeking and buffer
management — i.e. the media stack the project deliberately avoids.

**Status — initial MSE append path shipped (commit `2b71c0d`).** The browser
now exposes `MediaSource`, `SourceBuffer`, and `SourceBufferList` from
`data/js/polyfills.js`; `URL.createObjectURL(mediaSource)` creates a native
blob URL; `SourceBuffer.appendBuffer()` publishes appended bytes back through
`src/js.c::__ndUpdateBlobURL`; `src/net.c` can fulfill async `blob:` fetches;
and `src/video.c` discovers `blob:` video sources. The libav-backed decoder
path in `src/video_decode.c` was widened from WebM-only VP8/VP9 to common
libav-backed containers/codecs (MP4/WebM-style input with VP8/VP9/H.264/HEVC/
AV1/MPEG-4/Theora, when libav is present). Headless smoke verified the MSE
surface: `MediaSource` opens, a buffer append fires `updateend`, the media
element is retargeted to a versioned blob URL, and `endOfStream()` reaches
`ended`.

**What this actually supports:** page scripts can create a `MediaSource`,
add a source buffer, append bytes, and hand the accumulated byte stream to
the existing inline video path through a blob URL. For split DASH streams,
the publisher deliberately selects the video `SourceBuffer` rather than
concatenating audio and video buffers into an invalid container. That means
the best current outcome is silent inline video when the appended video
bytestream is a complete supported container and libav/pl_mpeg can decode it.

**Still not full YouTube/DASH:** there is no true fragmented-MP4/WebM
incremental demuxer, no A/V sync across separate SourceBuffers, no adaptive
quality switcher, no segment eviction/pruning, no cross-segment seek, no EME
or DRM, and no guarantee against YouTube player churn. The Windows build used
for this pass did not have libav installed, so MP4/WebM decode coverage is
compile-gated by available FFmpeg packages even though the code path is now
ready.

Recommendation after the override: keep the shipped MSE path narrow and
honest. Use it as a compatibility bridge for sites whose appends produce a
complete playable bytestream, keep the external-player handoff for the hard
cases, and only expand toward full adaptive DASH if "daily-driver for video
sites" becomes an explicit product goal.

### POP/IMAP mail client, Netscape-Communicator style  ·  ✅ MVP + compose SHIPPED (1.0.17-dev)

Reality check (original): a Messenger-style client is IMAP4 + POP3 + SMTP,
MIME/RFC822 parsing, a local mail store, a three-pane UI (folders / message
list / reader), a compose window, and credential storage — a sizeable
application bolted onto the browser that stretches the "small enough for one
person to audit end-to-end" constraint. The bounded first step shipped, and
went one increment past the read-only MVP to include sending.

- **Status — shipped (commit `67aba1f`).** A working `about:email` client
  landed. `src/mail.c` (`ns_mail_*`) drives IMAP and POP3 inbox sync and
  SMTP send entirely over libcurl's native `imaps://`/`pop3s://`/
  `smtp(s)://` support, on worker threads with the `about:ai`-style
  idle/running/done/error poll model. The UI is HTML served from the
  `about:` router (mirroring `about:ai`): `about:email` carries inbox,
  reader, compose and account-setup views (deep links `about:email?uid=N`,
  `about:email?compose=1`). MIME handling covers multipart traversal,
  quoted-printable/base64 decode, RFC 2047 header decode, charset→UTF-8 and
  a text/plain-preferred extraction with an HTML-to-text fallback; SMTP send
  assembles an RFC 5322 message with dot-stuffing and RFC 2047 subject/name
  encoding. A single account persists to `~/.config/nordstjernen/email.conf`
  (`0600`). An envelope toolbar button / Email menu item / `Ctrl+M` open the
  client. The same change **ported the general Settings dialog from GTK to
  an HTML `about:settings` page** (so both dialogs now share the
  HTML-over-`about:` UI system). **Verified** end-to-end against live local
  SMTP and IMAP servers (send capture; inbox list with RFC 2047 decode;
  multipart read with QP decode); an adversarial review caught and fixed a
  NULL-deref on an empty quoted-printable part plus a config-write data
  race (`ns_config_lock`/`unlock`).
- **Protocol layer was indeed mostly free:** libcurl's mail schemes needed
  only wiring over the existing curl conventions (`ns_net_apply_curl_tls`,
  the shared write-callback pattern, the `g_uri_parse_params` form
  parsing) — no new network stack. The about: router only needed
  `email-*`/`settings-*` endpoints (chrome-only gated) and POST bodies
  threaded through `synthesize_about_response`.
- **Delivered:** the MVP (configure an account, list the INBOX, fetch and
  render one message with multipart MIME) **plus compose + send over SMTP**
  — i.e. read *and* write, one step past the originally-scoped first step.
- **Remaining toward a full Communicator Messenger (long-horizon, not
  started):** multiple accounts, folders beyond INBOX, an offline/cached
  store, search, an address book, filter rules, and HTML-mail sanitization
  for rendering remote HTML safely (today the reader strips HTML to text,
  which is safe but plain). **Credential storage is plaintext at `0600`**
  (matching the existing cookie-jar posture); an OS-keyring or
  AES-at-rest-over-`webcrypto.c` option is the obvious next hardening.

Ethos note (unchanged): bundling mail expands the audit surface in exactly
the way modern minimal browsers avoid. The MVP + compose step is shipped and
bounded (`src/mail.c` is self-contained, single account, INBOX-only);
whether to push toward a full Communicator-style suite remains a *product*
decision, not a feature drop.


## Watch-outs & non-goals

- **Don't steer by the raw WPT ROI table before rank 1 lands** — its top
  two rows credit tentative SHA-3/cSHAKE/K12 and `OpaqueRange`, explicit
  non-goals.
- **`https`-redirect pinning must be conditional** on the request having
  started `https`, or it breaks currently-working plain-http SSE.
- **Sequence rank 4 strictly before the `:has()` scoping.** Its
  intermittent, input-dependent stale-paint bugs are exactly what the
  no-automated-test policy makes nearly invisible to manual verification.
- **Storage is already bounded** — `localStorage` 5 MiB quota
  (`js.c:1965-2058`), HTTP cache `evict_to_cap` (`cache.c:644`), per-IDB-DB
  page cap (`idb.c:170`). The only residual is an aggregate per-origin IDB
  cap (folded into rank 10), not a headline DoS dimension.
- **The no-JIT interpreter is not a perf bug** — it is a deliberate W^X
  security choice. All measured perf wins to date are in the CSS cascade,
  not `js.c`; aim perf work there (the `:has()` and subtree-relayout bets).
- **Flex baseline is not a quick win** — baseline alignment needs each
  item's first line-box ascent, a post-layout used value, so it is a second
  pass bolted onto a deliberately single-pass flex implementation
  (`layout.c:7339-7363`). The cheap part is only the doc correction.
- **innerText layout-accuracy** (122-126 subtests) looks dense but is
  deeply layout-coupled for largely whitespace edge cases — high subtest
  count, low product impact. Don't let its WPT number pull it above
  URL/IDNA.
- **Next audit hot-spots:** `src/ext.c` (filter-list parser, 4 commits old)
  and `src/webgpu.c` (already produced one detach-during-`valueOf`
  finding) — fast-written code over attacker-adjacent bytes. Route both
  through the rank-4 harness first.
- **Editing the right layer:** Range/TreeWalker/NodeIterator are **live in
  `data/js/polyfills.js`** and the C path is dead — edit the polyfill, not
  the C stub. Selection is the exception (its C stub is live), so it must
  be implemented (rank 8), not edited away.
- **Conscious non-goals:** Web Audio synthesis, getUserMedia/RTC, and full
  WebGPU completion remain large and against the no-media-stack ethos, or
  behind an experimental opt-in gate. MSE/SourceBuffer is no longer a pure
  non-goal after `2b71c0d`, but only as the narrow append-to-blob bridge
  documented above; full adaptive DASH/HLS, A/V sync and DRM remain outside
  the 1.0.17 scope unless promoted to an explicit product goal. One cheap
  honesty fix: make `getByteFrequencyData` return zeros instead of throwing
  so loose `try/catch` visualizers don't break.
- **Every fix is a manual-verify event** — budget smoke-launch time per
  change and use `--headless --dump` against `data/render-tests` fixtures.
